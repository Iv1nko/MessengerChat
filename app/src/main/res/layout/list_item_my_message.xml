<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >

    <TextView
        android:id="@+id/message_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:text="time"
        android:layout_alignParentRight="true"/>

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/image_user_chat_log_right"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_below="@+id/message_time"
        android:layout_marginStart="7dp"
        android:layout_marginTop="7dp"
        android:background="@drawable/ic_profile_image"
        android:layout_alignParentEnd="true"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:civ_border_width="2dp"


        />

    <com.github.library.bubbleview.BubbleTextView
        android:id="@+id/your_message_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/message_time"
        android:layout_marginTop="7dp"
        android:layout_toStartOf="@+id/image_user_chat_log_right"
        android:maxWidth="240dp"
        android:padding="10dp"
        android:text="this is my message"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        style="@style/normalText"
        android:textSize="18sp"
        app:angle="10dp"
        app:arrowLocation = "right"
        app:arrowPosition="10dp"
        app:arrowWidth="8dp"
        app:bubbleColor="@color/message_color" />

</RelativeLayout>
