<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:icon="@drawable/ic_baseline_menu_free_point"
        app:showAsAction="ifRoom"
        android:title="">
        <menu>
            <item
                android:id="@+id/settings_menu_change_name"
                android:icon="@drawable/ic_baseline_change_name"
                android:title="@string/change_name"
                app:showAsAction="never"
                />
            <item
                android:id="@+id/settings_menu_change_password"
                android:icon="@drawable/ic_baseline_change_password"
                android:title="@string/change_password"
                app:showAsAction="never"
                />
        </menu>
    </item>
</menu>